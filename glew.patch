diff -aurN glew-1.10.0/include/GL/glew.h glew-1.10.0-fixed/include/GL/glew.h
--- glew-1.10.0/include/GL/glew.h	2013-07-22 07:11:22 -0700
+++ glew-1.10.0-fixed/include/GL/glew.h	2013-07-31 21:51:21 -0700
@@ -187,20 +187,7 @@
 #define GLEWAPIENTRY APIENTRY
 #endif
 
-/*
- * GLEW_STATIC is defined for static library.
- * GLEW_BUILD  is defined for building the DLL library.
- */
-
-#ifdef GLEW_STATIC
 #  define GLEWAPI extern
-#else
-#  ifdef GLEW_BUILD
-#    define GLEWAPI extern __declspec(dllexport)
-#  else
-#    define GLEWAPI extern __declspec(dllimport)
-#  endif
-#endif
 
 #else /* _UNIX */
 
diff -aurN glew-1.10.0/include/GL/wglew.h glew-1.10.0-fixed/include/GL/wglew.h
--- glew-1.10.0/include/GL/wglew.h	2013-07-22 07:11:22 -0700
+++ glew-1.10.0-fixed/include/GL/wglew.h	2013-07-31 21:51:21 -0700
@@ -70,19 +70,7 @@
 #  undef WIN32_LEAN_AND_MEAN
 #endif
 
-/*
- * GLEW_STATIC needs to be set when using the static version.
- * GLEW_BUILD is set when building the DLL version.
- */
-#ifdef GLEW_STATIC
 #  define GLEWAPI extern
-#else
-#  ifdef GLEW_BUILD
-#    define GLEWAPI extern __declspec(dllexport)
-#  else
-#    define GLEWAPI extern __declspec(dllimport)
-#  endif
-#endif
 
 #ifdef __cplusplus
 extern "C" {
